// Maior número

function MaiorNumero (num1, num2, num3) {
 return Math.max(num1, num2, num3);
}

console.log(MaiorNumero(2659,353,44))

// Menor Número

function menorNumero(numeros) {
    return Math.min(...numeros);
}

console.log(menorNumero([945, 5, 7, 9, 12461]));
